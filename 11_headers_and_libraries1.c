/*
	Headers and libraries
*/

/* system headers are included with <> */
#include <stdio.h> 

/* user defined headers are included with "" */
#include "11_headers_and_libraries.h"

int main()
{

	printf("max(3, 5) = %d\n", max(3, 5));
	printf("square(3) = %d\n", square(3));
	printf("SIZE = %d\n", SIZE);

}